package com.javapub.demo.elasticsearch.springbootelasticsearch.enums;

/**
 * 字段类型
 */
public enum ESType {
    KEYWORD, TEXT, DATE, BOOLEAN, INTEGER, LIST, JSON, FLOAT, OBJECT, NEST_LIST, RTE, DOUBLE
}
